{% assign_local c = "c-email-input" %}
<div class="{{ c }}"{% if rosey_ns != nil %} data-rosey-ns="field-{{ rosey_ns }}"{% endif %}>
    <label class="{{ c }}__label" for="{{ id }}"><span data-rosey="label">{{ label }}</span>{% if required %} *{% endif %}</label>
    <input class="{{ c }}__input"
        type="email" 
        id="{{ id }}" 
        name="_replyto" 
        placeholder="{{ placeholder }}"
        {% if required %}required{% endif %}
        onblur="validateInput(this)"
        oninput="validateInputTyping(this)"
        {% if helper_text %}aria-describedby="{{ id }}--helper"{% endif %}>

    {% if helper_text %}
        {% bookshop "generic/form/helper-text" id={{ id }} helper_text={{ helper_text }} %}
    {% endif %}

    {% bookshop "generic/form/error-message" id={{ id }} %}
</div>